a tiny bootloader similar to master boot record(MBR) that exists in the first 512 bytes

```
make assemble # to assemble .s files
make disk # to create a tiny .img file
make qemu # to start the qemu emulator
```

> wrote the bootloader for my operating system's course assignment
